# Research-machine dependencies — pinned to the working environment
# (Python 3.14, numpy 2.x, pandas 3.x). Regenerated from the venv that
# the code + tests actually run on; the previous file was a stale 3.9-era
# freeze that no longer installs and listed unused deps (vectorbt,
# statsmodels, hmmlearn) the code never imports.
#
# TA-Lib needs its C library installed separately (brew install ta-lib).
# Dev tooling (ruff, pytest) lives in requirements-dev.txt; the lean
# execution-only set for the server is in requirements-bot.txt.

alembic==1.18.4
build==1.4.4
certifi==2026.4.22
charset-normalizer==3.4.7
cloudpickle==3.1.2
colorlog==6.10.1
contourpy==1.3.3
cycler==0.12.1
duckdb==1.5.5
fonttools==4.62.1
idna==3.15
joblib==1.5.3
kiwisolver==1.5.0
lightgbm==4.6.0
llvmlite==0.47.0
Mako==1.3.12
MarkupSafe==3.0.3
matplotlib==3.10.9
numba==0.65.1
numpy==2.4.4
optuna==4.9.0
packaging==26.2
pandas==3.0.2
pillow==12.2.0
pyarrow==24.0.0
pyparsing==3.3.2
pyproject_hooks==1.2.0
psycopg[binary]==3.3.4
python-dateutil==2.9.0.post0
PyYAML==6.0.3
requests==2.33.1
scikit-learn==1.8.0
scipy==1.17.1
shap==0.52.0
six==1.17.0
slicer==0.0.8
SQLAlchemy==2.0.50
TA-Lib==0.6.8
threadpoolctl==3.6.0
tqdm==4.67.3
typing_extensions==4.15.0
urllib3==2.7.0
